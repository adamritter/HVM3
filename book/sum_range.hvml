data List { #Nil #Cons{ head tail } }

@sum(xs r) = ~xs{
  #Nil: r
  #Cons: λhead λtail @sum(tail (+ head r))
}

@range(n xs) = ~n{
  #0: xs
  #_: λpred
    ! &0{n0 n1} = pred
    @range(n0 #Cons{n1 xs})
}

@main = @sum(@range(50_000_000 #Nil) 0)
